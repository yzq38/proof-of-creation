# =============================================================================
# Proof Of Creation (PoC) 服务端配置
# 服务器启动前必须确保此文件存在且所有参数完备合法
# =============================================================================

# ---------------------------------------------------------------------------
# 区块链与合约配置
# ---------------------------------------------------------------------------

# 以太坊节点 RPC URL (Sepolia 测试网)
NODE_URL=https://ethereum-sepolia-rpc.publicnode.com

# 区块链 ID (Sepolia = 11155111)
CHAIN_ID=11155111

# 服务器钱包私钥 (仅用于支付 Gas 费，储存 Sepolia ETH)
PRIVATE_KEY=0x你的钱包私钥

# BasicRecord 合约地址 (部署后填写)
CONTRACT_BASIC_ADDRESS=0x部署后填写

# AdvancedRecord 合约地址 (部署后填写)
CONTRACT_ADVANCED_ADDRESS=0x部署后填写

# ---------------------------------------------------------------------------
# 定价参数
# ---------------------------------------------------------------------------

# 固定手续费 (人民币元)
FIXED_FEE=0.20

# 比例手续费 (5%)
FEE_RATE=0.05

# Gas 上限最小倍率 (用户可自行调高)
MIN_GAS_MULTIPLIER=1.2

# 支付价格倍率 (预估总Gas费 × 此倍率 + 固定手续费)
PRICE_MULTIPLIER=1.5

# 订单有效期 (秒)
ORDER_EXPIRY=60

# ---------------------------------------------------------------------------
# 汇率
# ---------------------------------------------------------------------------

# ETH/CNY 汇率 API 地址
EXCHANGE_RATE_API=https://cny.rate.sx/1eth

# 汇率缓存时间 (秒)，避免频繁请求
EXCHANGE_CACHE_TTL=30

# 汇率兜底值 (API 不可用时的备用汇率)
FALLBACK_RATE=15000

# ---------------------------------------------------------------------------
# 服务端配置
# ---------------------------------------------------------------------------

# 监听地址
HOST=127.0.0.1

# 监听端口
PORT=8080

# ---------------------------------------------------------------------------
# JWT 认证配置
# ---------------------------------------------------------------------------

# JWT 签名密钥 (务必修改为随机强密码)
JWT_SECRET=修改为随机生成的强密码(至少32字节)

# JWT Token 有效期 (秒)
JWT_EXPIRY=86400

# ---------------------------------------------------------------------------
# 数据库配置
# ---------------------------------------------------------------------------

# SQLite 数据库文件路径
DATABASE_PATH=data/poc.db

# ---------------------------------------------------------------------------
# 日志配置
# ---------------------------------------------------------------------------

# 日志文件目录
LOG_DIR=logs

# 日志级别: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO

# HTTPS 证书 (泛域名证书路径, 留空则使用 HTTP)
SSL_CERT=ssl/cert.pem
SSL_KEY=ssl/key.pem
